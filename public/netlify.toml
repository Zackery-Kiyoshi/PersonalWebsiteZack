[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--include=dev"

# Optional: give Netlify a clear SPA rule too (redundant if you ship public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
